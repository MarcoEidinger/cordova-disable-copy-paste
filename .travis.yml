language: objective-c
install:
  - npm install -g cordova
  - npm install -g ios-sim
  - npm install -g ios-deploy
  - npm install -g cordova-paramedic
  - npm install
script:
  - npm test
  - cordova-paramedic --platform ios --plugin https://github.com/MarcoEidinger/cordova-disable-copy-paste
